fragment Image on FileField {
    responsiveImage(imgixParams: {auto: format, fit: crop}) {
        alt
        aspectRatio
        base64
        height
        sizes
        src
        srcSet
        width
    }
}
